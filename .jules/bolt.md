## 2024-07-24 - Keyword Classification Overhead
**Learning:** Initializing long lists of string keywords inside frequently called text-matching functions (like `gorev_tipini_belirle`) causes unnecessary list creation and garbage collection overhead on every invocation.
**Action:** Move static keyword lists out of function scope and into globally defined immutable structures (`tuple`) to reuse memory, ensuring faster lookup time.