# dijital-varlik — Temel Bagimliliklar
# ======================================
# Kendi kendine kurulum: pip install -r requirements.txt

# Temel
requests>=2.31
httpx>=0.27
aiohttp>=3.9
pydantic>=2.0
python-dotenv>=1.0
pyyaml>=6.0

# LiteLLM — LLM proxy/router
litellm>=1.0

# Mem0 — Semantik bellek (vektor tabanli)
mem0ai>=0.1.0

# smolagents — Code Agent motoru
smolagents>=0.1.0

# Browser-Use — Otonom tarayici
browser-use>=0.1.0
browser-use-sdk>=3.0

# Ses (TTS + STT)
faster-whisper>=1.0
sounddevice>=0.5
# F5-TTS ve Pipecat GPU gerektirir, opsiyonel
# f5-tts>=0.1.0
# pipecat-ai>=0.1.0

# Web sunucu (opsiyonel kokpit)
fastapi>=0.100
uvicorn>=0.30

# LangGraph (opsiyonel — gelismis akis kontrolu)
# langgraph>=0.2.0
